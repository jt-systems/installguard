{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "AdvisorySeverity": {
      "description": "Severity floor for the [`Reason::AdvisoryKnown`] gate.\n\nSeverities form a total order; setting the floor at `high` means any advisory of `high` *or* `critical` fires the gate. `none` disables the gate entirely — advisories are still recorded as signals but are never promoted to reasons.\n\n`unknown` is a separate sentinel for advisories whose source did not record a severity. We treat it as below `low` so an operator who sets the floor to `low` does NOT accidentally block on every unrecorded-severity advisory.",
      "enum": [
        "none",
        "unknown",
        "low",
        "medium",
        "high",
        "critical"
      ],
      "type": "string"
    },
    "Defaults": {
      "additionalProperties": false,
      "properties": {
        "blockExoticSubdeps": {
          "default": false,
          "description": "Block dependencies whose source is not a registry or workspace.",
          "type": "boolean"
        },
        "detectPublisherChange": {
          "default": false,
          "description": "Emit a [`Reason::PublisherChange`] when the resolved version was published by a different npm account than the immediately-prior version. Default severity is `block`; downgrade via the `severity` map (e.g. `publisher-change: warn`).",
          "type": "boolean"
        },
        "detectVersionSurfaceChange": {
          "default": false,
          "description": "Emit a [`Reason::VersionSurfaceChange`] when the resolved version adds new `bin` entries or new lifecycle-script names compared to the immediately-prior released version. Off by default — legitimate releases regularly add CLIs — but extremely useful when scoped to direct deps via `direct.detectVersionSurfaceChange: true`.",
          "type": "boolean"
        },
        "flagDeprecated": {
          "default": false,
          "description": "Emit a [`Reason::DeprecatedVersion`] when the registry reports the resolved version as deprecated. Default severity is `block`; many projects will want `severity.deprecated-version: warn` during a clean-up rollout.",
          "type": "boolean"
        },
        "maxAdvisorySeverity": {
          "allOf": [
            {
              "$ref": "#/definitions/AdvisorySeverity"
            }
          ],
          "default": "none",
          "description": "Lowest advisory severity that should fire [`Reason::AdvisoryKnown`]. Advisories below this floor are recorded on the dependency for visibility but do not block. Defaults to [`AdvisorySeverity::None`] (off) so adopting the OSV provider is opt-in. Recommended starting point: `maxAdvisorySeverity: high`."
        },
        "minMaintainerAccountAgeDays": {
          "default": 0,
          "description": "Block versions whose publisher account is younger than this many days at the time of publication. `0` disables the check. Account-takeover attacks frequently use freshly-created burner accounts; a 30- to 60-day floor catches them without flagging legitimate new maintainers in steady-state projects.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "minTrustScore": {
          "default": 0,
          "description": "Minimum aggregate trust score in `[0, 100]` (see [`crate::trust_score`]). `0` disables the check. The score is computed from the full signal set after every other detector has run; it acts as a *cumulative* gate that catches dependencies whose individual signals each look acceptable but together cross a risk budget.",
          "format": "uint8",
          "minimum": 0.0,
          "type": "integer"
        },
        "minimumReleaseAge": {
          "default": null,
          "description": "Minimum release age in minutes. `0` disables the check.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "requireProvenance": {
          "default": false,
          "description": "Require the dependency to carry verified npm provenance (a Sigstore DSSE bundle whose in-toto subject digest matches the tarball integrity). When `true`, missing or unverifiable provenance is reported as [`Reason::ProvenanceMissing`]. Off by default — most of the npm ecosystem does not yet publish with `--provenance`, so global enforcement breaks builds. Recommended scope: `direct.requireProvenance: true` for first-party deps in regulated environments.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DirectOverrides": {
      "additionalProperties": false,
      "properties": {
        "detectPublisherChange": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::detect_publisher_change`]. When `Some(_)`, takes precedence for direct deps.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "detectVersionSurfaceChange": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::detect_version_surface_change`].",
          "type": [
            "boolean",
            "null"
          ]
        },
        "flagDeprecated": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::flag_deprecated`].",
          "type": [
            "boolean",
            "null"
          ]
        },
        "maxAdvisorySeverity": {
          "anyOf": [
            {
              "$ref": "#/definitions/AdvisorySeverity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::max_advisory_severity`]."
        },
        "minMaintainerAccountAgeDays": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::min_maintainer_account_age_days`].",
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "minTrustScore": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::min_trust_score`].",
          "format": "uint8",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "minimumReleaseAge": {
          "default": null,
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "requireProvenance": {
          "default": null,
          "description": "Per-direct-dep override for [`Defaults::require_provenance`].",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ScriptPolicy": {
      "enum": [
        "deny-by-default",
        "allow-by-default"
      ],
      "type": "string"
    },
    "Scripts": {
      "additionalProperties": false,
      "properties": {
        "allow": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "policy": {
          "allOf": [
            {
              "$ref": "#/definitions/ScriptPolicy"
            }
          ],
          "default": "deny-by-default"
        }
      },
      "type": "object"
    },
    "Severity": {
      "description": "Severity assigned to a `Reason` by the policy. `Allow` suppresses the reason entirely; `Warn` records it but does not fail; `Block` fails.",
      "enum": [
        "allow",
        "warn",
        "block"
      ],
      "type": "string"
    }
  },
  "description": "Policy DSL for InstallGuard. See DESIGN.md §4 for full semantics.",
  "properties": {
    "defaults": {
      "allOf": [
        {
          "$ref": "#/definitions/Defaults"
        }
      ],
      "default": {
        "blockExoticSubdeps": false,
        "detectPublisherChange": false,
        "detectVersionSurfaceChange": false,
        "flagDeprecated": false,
        "maxAdvisorySeverity": "none",
        "minMaintainerAccountAgeDays": 0,
        "minTrustScore": 0,
        "minimumReleaseAge": null,
        "requireProvenance": false
      }
    },
    "direct": {
      "allOf": [
        {
          "$ref": "#/definitions/DirectOverrides"
        }
      ],
      "default": {
        "detectPublisherChange": null,
        "detectVersionSurfaceChange": null,
        "flagDeprecated": null,
        "maxAdvisorySeverity": null,
        "minMaintainerAccountAgeDays": null,
        "minTrustScore": null,
        "minimumReleaseAge": null,
        "requireProvenance": null
      }
    },
    "policyVersion": {
      "format": "uint32",
      "minimum": 0.0,
      "type": "integer"
    },
    "scripts": {
      "allOf": [
        {
          "$ref": "#/definitions/Scripts"
        }
      ],
      "default": {
        "allow": [],
        "policy": "deny-by-default"
      }
    },
    "severity": {
      "additionalProperties": {
        "$ref": "#/definitions/Severity"
      },
      "default": {},
      "description": "Per-reason severity overrides. Keys are kebab-case reason codes (e.g. `release-age-below-threshold`); values are `allow` (suppress), `warn`, or `block`. Reasons not listed keep their default severity (currently always `block`, preserving v1 semantics).",
      "type": "object"
    }
  },
  "required": [
    "policyVersion"
  ],
  "title": "InstallGuard policy",
  "type": "object"
}
